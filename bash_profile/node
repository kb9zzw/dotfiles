export NVM=/usr/local/opt/nvm
export NVM_DIR="$HOME/.nvm"

mkdir -p $NVM_DIR
[ -s "${NVM}/nvm.sh" ] && . "${NVM}/nvm.sh"  # This loads nvm
[ -s "${NVM}/etc/bash_completion.d/nvm" ] && . "${NVM}/etc/bash_completion.d/nvm"  # This loads nvm bash_completion
